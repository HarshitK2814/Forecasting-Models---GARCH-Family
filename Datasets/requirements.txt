# Data acquisition environment - GARCH-EVT / Realized GARCH / Quantile Regression project
# Researcher A. Verified working 2026-08-22 on Python 3.12 / Windows 11.
#
# certifi is PINNED DELIBERATELY. The workstation shipped certifi 2024.08.30, whose CA
# bundle had expired roots - every HTTPS fetch failed with CERTIFICATE_VERIFY_FAILED.
# Do not let it drift backwards.

certifi>=2026.7.22
requests>=2.32
pandas>=2.2
numpy>=1.26
yfinance>=0.2.65
xlsxwriter>=3.2
openpyxl>=3.1

# Optional: columnar mirror of the large intraday tables
pyarrow>=15.0

# Modelling stage. In active use from 2026-08-24 (baseline GARCH/GJR/EGARCH, Realized GARCH,
# rolling forecast engine, robustness checks - see 10_SCRIPTS/27-31 and RESEARCHER_A_SCOPE.md).
# `arch` has no Realized GARCH built in; 28_realized_garch.py implements it directly on top
# of scipy.optimize + scipy.stats.
arch>=7.0
statsmodels>=0.14
scipy>=1.11
matplotlib>=3.8
tabulate>=0.9      # pandas.DataFrame.to_markdown, used by 30_robustness_checks.py
