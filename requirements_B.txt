# Researcher B modelling environment, captured 2026-08-24 on Colab.
# Pinned because quantile-regression breach counts can differ by one day across
# scipy/numpy versions where a return falls within ~1e-5 of the fitted VaR.
# Observed: NKY QR-Range 36 vs 37 breaches of 3,037 (Kupiec p 0.319 vs 0.242).
# No test verdict changes, but the reference run is this environment.
python==3.13.15
statsmodels==0.14.6
scipy==1.16.3
numpy==2.1.3
pandas==2.2.3
arch
matplotlib
